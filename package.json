{
  "name": "water-purifier-mobile",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "ios:prepare": "node scripts/ios-build.mjs prepare-mac",
    "ios:archive:unsigned": "node scripts/ios-build.mjs archive-unsigned",
    "ios:archive:signed": "node scripts/ios-build.mjs archive-signed",
    "ios:export:development": "node scripts/ios-build.mjs export-development",
    "cap:add:ios": "npx cap add ios",
    "cap:sync": "npx cap sync",
    "cap:open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.2",
    "@capacitor/ios": "^7.4.2",
    "typescript": "^5.8.3",
    "vite": "^7.1.0"
  }
}
